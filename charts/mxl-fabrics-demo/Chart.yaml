apiVersion: v2
name: mxl-fabrics-demo
description: >-
  MXL Fabrics API demo — a target and an initiator running mxl-fabrics-demo
  over the libfabric tcp provider. placementMode selects the shape:
  split-node (two media nodes, hostNetwork over the VPC private plane) or
  single-node (colocated on one node over the pod network).
type: application
version: 0.4.0
appVersion: "1.0.3"
