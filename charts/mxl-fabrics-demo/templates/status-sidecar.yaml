apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mxl-fabrics-demo.name" . }}-status-sidecar
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mxl-fabrics-demo.labels" . | nindent 4 }}
    dmf.component: mxl-status-sidecar
data:
  mxl-status-server.py: |
{{ .Files.Get "files/mxl-status-server.py" | nindent 4 }}
